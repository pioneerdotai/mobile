{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ClientInvitationCommitState",
  "type": "string",
  "enum": [
    "refresh_ready",
    "awaiting_secure_storage",
    "awaiting_registry",
    "durable_session_unbound",
    "ready_to_connect"
  ]
}